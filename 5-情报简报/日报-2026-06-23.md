# 情报日报 — 2026-06-23

## 采集状态

所有数据源爬取失败，今日无有效数据可分析：

| 数据源 | 错误 |
|-------|------|
| Hacker News | SSL: UNEXPECTED_EOF_WHILE_READING |
| GitHub Trending | SSL: UNEXPECTED_EOF_WHILE_READING |
| ArXiv | failed to fetch |
| Product Hunt | failed to fetch |

原因：采集脚本执行时网络层或 SSL/TLS 握手异常，可能是代理/证书问题或目标 API 短时不可用。建议检查：

1. WSL 网络代理配置是否正常（`curl -I https://news.ycombinator.com` 测试）
2. SSL 证书链是否需要更新（`update-ca-certificates`）
3. 是否触发了目标站点的限流

明日待正常采集后恢复正常日报产出。

---

📊 **运行统计**：耗时 <1 分钟 · 输入 ~300 tokens · 输出 ~200 tokens
